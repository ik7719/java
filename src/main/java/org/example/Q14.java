package org.example;

public class Q14 {
    public static void main(String[] args){
        int arr[] = new int[] {5, 9, 7, 10};
        int divisor = 5;
        int answer = 0;

        for(int i = 0; i < arr.length; i++) {
            if(arr[i] / divisor == 0){
                // 그 값을 반환해라
                // 아니면 나눈 값을 저장하고 그 값을 다시 divisor로 곱한 후 배열로 저장
            }
        }
    }
}
